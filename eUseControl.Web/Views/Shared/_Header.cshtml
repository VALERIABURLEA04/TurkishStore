<nav class="navbar navbar-expand-lg  navbar-light bg-light">
    <div class="container">
        <a class="navbar-brand" href="#">
            <img src="@Url.Content("~/Content/images/photo_2025-02-24_16-47-55.jpg")" alt="Logo" class="logo-img" />
        </a>

        <button class="btn btn-primary m-3" id="buton_menu" type="button" data-bs-toggle="offcanvas" data-bs-target="#sidebar">

            <i class="bi bi-cart" style="font-size: 1.5rem; color: black;"></i> <!-- Iconița hamburger -->
        </button>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
        <button style="font-size:60px;" class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-center" id="navbarNav">
            <ul class="navbar-nav mx-auto">

                <li class="nav-item mx-3"><a class="nav-link" href="@Url.Action("Index", "Home")">Home</a></li>

                <!-- Link pentru About Us -->
                <li class="nav-item mx-3"><a class="nav-link" href="@Url.Action("About", "Home")">About Us</a></li>

                <li class="nav-item mx-3"><a class="nav-link" href="@Url.Action("Shop", "Shop")">Shop </a></li>
                <li class="nav-item mx-3"><a class="nav-link" href="@Url.Action("NewIn", "Home")">New In</a></li>
                <li class="nav-item mx-3"><a class="nav-link" href="@Url.Action("ContactUs", "Home")">Contact Us</a></li>

                @if (Session["Role"]?.ToString() == "Admin")
                {
                    <li class="nav-item mx-3"><a class="nav-link" href="@Url.Action("ProductList", "Admin")">Manage Products</a></li>
                }
            </ul>
        </div>

        <!-- Navbar Icons -->
        <div class="navbar-icons d-flex align-items-center gap-3 ms-auto">

            <div class="nav-item dropdown">
                <a class="nav-link dropdown-toggle text-dark" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    <i class="fas fa-user"></i> @(Session["UserFirstName"] ?? "My Account")
                </a>
                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="userDropdown">
                    @if (Session["UserFirstName"] == null)
                    {
                        <a class="dropdown-item" href="@Url.Action("Login", "Auth")">Login</a>
                    }
                    else
                    {
                        <a class="dropdown-item" href="@Url.Action("Profile", "Auth")">Profil</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item text-danger" href="@Url.Action("Logout", "Auth")">Logout</a>
                    }
                </div>
            </div>

            <a href="#" class="nav-link text-dark" title="Search">
                <i class="fas fa-search"></i>
            </a>

            <a href="@Url.Action("Favorites", "Shop")" class="nav-link text-dark" title="Favorites">
                <i class="fas fa-heart"></i>
            </a>

            @if (Session["AdminUsername"] != null)
            {
                <a href="@Url.Action("Logout", "Admin")" class="btn btn-sm btn-outline-danger ms-2">
                    <i class="fas fa-sign-out-alt me-1"></i> Logout
                </a>
            }
        </div>
    </div>
</nav>